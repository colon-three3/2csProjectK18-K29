// Test function
function emotionalPopup() {
   if (pageYOffset == 0) {
     console.log("*goofy emotional pop up emotionally Pops up* uwu");
     console.log("pageYOffset =" + pageYOffset);
     console.log("emotionalPopup() successful.")
     popup1();  
     }
}
window.addEventListener('scroll', emotionalPopup);

function ehsAudio() {
  const ehsAudios = {
  document.getElementById('ehs1');
  }
}

function ehsDelay() {
  setTimeout(emotionalPopup, 3000)
  ehsAudio();
}






//The pageYOffset property returns the pixels a document has scrolled from the upper left corner of the window.